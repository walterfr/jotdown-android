<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        JOTDOWN — Fichador Acadêmico
        Licença: GPL-3.0-only
        Autor: @prof.walterfr

        PERMISSÕES:
        Nenhuma permissão sensível é solicitada.
        O app funciona 100% offline com dados armazenados no IndexedDB local.
        Os PDFs são carregados pelo seletor de arquivos padrão do Android
        (não requer permissão READ_EXTERNAL_STORAGE no Android 13+).
    -->

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Jotdown"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
